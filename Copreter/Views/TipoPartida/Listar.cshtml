@model TTipoPartidum;
@{
    ViewData["Title"]="Index";
}

    <!-- Content Header (Page header) -->
    <section class="content-header">
      <div class="container-fluid">
        <div class="row mb-2">
          <div class="col-sm-6">
            <h1>Lista de Tipo de partidas</h1>
          </div>
          <div class="col-sm-6">
            <ol class="breadcrumb float-sm-right">
              <li class="breadcrumb-item"><a href="#">Home</a></li>
              <li class="breadcrumb-item active"> Tipo partidas</li>
            </ol>
          </div>
        </div>
      </div><!-- /.container-fluid -->
    </section>

    <!-- Main content -->
    <section class="content">
      <div class="container-fluid">
        <div class="row">
        
        </div>
        <!-- /.row -->
        <div class="row">
          <div class="col-12">
            <div class="card">
              <div class="card-header">
                <div class="card-tools">
                  <div class="input-group input-group-sm" style="width: 150px;">
                    <input type="text" name="table_search" class="form-control float-right" placeholder="Search">
                    <div class="input-group-append">
                      <button type="submit" class="btn btn-default">
                        <i class="fas fa-search"></i>
                      </button>
                    </div>
                  </div>
                </div>
              </div>
              <!-- /.card-header -->
              <div class="card-body table-responsive p-0">
                <table class="table table-hover text-nowrap">
                  <thead>
                    <tr>
                       <th style="text-align:center;">Item</th>
                        <th style="text-align:center;">Nombre de tipo partida </th>
                        <th style="text-align:center;">Descripción</th> 
                        <th colspan="3" style="text-align:center;" >Acciones</th>
                    </tr>
                  </thead>
                  <tbody>
                      @foreach (TTipoPartidum  item in ViewBag.TipoPartida)
                    {
                    <tr>
                    <td>@item.IdTipoPartida</td>
                    <td>@item.NombreTipoPartida</td>
                    <td>@item.DescripcionTipoPartida</td>
                    <td style = "text-align:center;"><a class= "btn btn-success" asp-action="Actualizar"  asp-route-id="@item.IdTipoPartida" >Modificar</a></td>
                  <!-- /  <td style = "text-align:center;"><a class= "btn btn-danger" asp-page="/Eliminar"  asp-route-id="@item.IdTipoPartida" >Borrar</a></td> -->
                    <td style = "text-align:center;" margen><a class= "btn btn-primary" asp-action="Detalle"  asp-route-id="@item.IdTipoPartida" >Ver Detalles</a></td>
                    </tr>
                    }
                  </tbody>
                </table>
                  <!-- /.card-body -->
              <div class="card-footer clearfix">
                <ul class="pagination pagination-sm m-0 float-right">
                  <li class="page-item"><a class="page-link" href="#">«</a></li>
                  <li class="page-item"><a class="page-link" href="#">1</a></li>
                  <li class="page-item"><a class="page-link" href="#">2</a></li>
                  <li class="page-item"><a class="page-link" href="#">3</a></li>
                  <li class="page-item"><a class="page-link" href="#">»</a></li>
                </ul>
              </div>
            </div>
              <td style = "text-align:left;"><a class= "btn btn-danger" asp-action="Agregar">Agregar</a></td>
            <!-- /.card -->
              </div>
              <!-- /.card-body -->
            </div>
            <!-- /.card -->
          </div>
        </div>

      </div><!-- /.container-fluid -->
    </section>
    <!-- /.content -->